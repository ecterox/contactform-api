<VirtualHost *:80>

    # Allow WebSocket (for HMR)
#    RewriteEngine On
#    RewriteCond %{HTTP:Upgrade} =websocket [NC]
#    RewriteRule /(.*) ws://192.168.200.40:3010/$1 [P,L]

    # Exclude Symfony paths
#    ProxyPassMatch ^/(api|media|bundles|_profiler|index\.php|favicon\.ico|robots\.txt) !

    # Pass everything else to Nuxt (even special characters like @id etc.)
#    ProxyPreserveHost On
#    ProxyPass / http://192.168.200.40:3010/ nocanon
#    ProxyPassReverse / http://192.168.200.40:3010/

    ServerAdmin webmaster@localhost

    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
    </Directory>

    DocumentRoot /var/www/html/public
    ErrorLog ${APACHE_LOG_DIR}/konfi_error.log
    CustomLog ${APACHE_LOG_DIR}/konfi_access.log combined

</VirtualHost>
